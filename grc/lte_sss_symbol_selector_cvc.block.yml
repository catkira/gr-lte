id: lte_sss_symbol_selector_cvc
label: sss symbol selector cvc
category: '[Lte]'
templates:
  imports: from gnuradio import lte
  make: lte.sss_symbol_selector_cvc(${fftl}, ${name})
parameters:
- id: fftl
  label: Fftl
  dtype: int
- id: name
  label: Name
  dtype: string
inputs:
- label: in
  domain: stream
  dtype: complex
outputs:
- label: out
  domain: stream
  dtype: complex
  vlen: ${ fftl }
file_format: 1
