id: lte_remove_cp_cvc
label: remove cp cvc
category: '[Lte]'
templates:
  imports: from gnuradio import lte
  make: lte.remove_cp_cvc(${fftl}, ${key}, ${name})
parameters:
- id: fftl
  label: Fftl
  dtype: int
- id: key
  label: Key
  dtype: string
- id: name
  label: Name
  default: ' "remove_cp_cvc"'
  dtype: string
inputs:
- label: in
  domain: stream
  dtype: complex
outputs:
- label: out
  domain: stream
  dtype: complex
  vlen: ${ fftl }
file_format: 1
