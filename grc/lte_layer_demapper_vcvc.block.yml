id: lte_layer_demapper_vcvc
label: layer demapper vcvc
category: '[Lte]'
templates:
  imports: from gnuradio import lte
  make: lte.layer_demapper_vcvc(${N_ant}, ${vlen}, ${style}, ${name})
parameters:
- id: N_ant
  label: antenna ports
  dtype: int
- id: vlen
  label: vector length
  dtype: int
- id: style
  label: RX style
  dtype: string
- id: name
  label: Name
  default: ' "layer_demapper_vcvc"'
  dtype: string
inputs:
- label: in
  domain: stream
  dtype: complex
  vlen: ${ vlen }
  multiplicity: 1
- domain: message
  id: N_ant
  optional: 1
outputs:
- label: out
  domain: stream
  dtype: complex
  vlen: ${ vlen }
  multiplicity: 1
file_format: 1
