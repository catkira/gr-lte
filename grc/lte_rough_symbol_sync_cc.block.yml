id: lte_rough_symbol_sync_cc
label: rough symbol sync cc
category: '[Lte]'
templates:
  imports: from gnuradio import lte
  make: lte.rough_symbol_sync_cc(${fftl}, ${vlen}, ${name})
parameters:
- id: fftl
  label: Fftl
  dtype: int
- id: vlen
  label: Vlen
  dtype: int
- id: name
  label: Name
  default: ' "rough_symbol_sync_cc"'
  dtype: string
inputs:
- label: in
  domain: stream
  dtype: complex
  vlen: ${ vlen }
outputs:
- label: out
  domain: stream
  dtype: complex
  vlen: ${ vlen }
file_format: 1
