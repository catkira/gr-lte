id: lte_pss_calculator_vcm
label: pss calculator vcm
category: '[Lte]'
templates:
  imports: from gnuradio import lte
  make: lte.pss_calculator_vcm(${fftl}, ${name})
parameters:
- id: fftl
  label: Fftl
  dtype: int
- id: name
  label: Name
  dtype: string
inputs:
- label: in
  domain: stream
  dtype: complex
  vlen: '72'
outputs:
- domain: message
  id: lock 
- domain: message
  id: half_frame
- domain: message
  id: N_id_2 
file_format: 1
