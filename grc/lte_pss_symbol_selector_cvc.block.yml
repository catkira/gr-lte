id: lte_pss_symbol_selector_cvc
label: pss symbol selector cvc
category: '[Lte]'
templates:
  imports: from gnuradio import lte
  make: lte.pss_symbol_selector_cvc(${fftl}, ${name})
parameters:
- id: fftl
  label: Fftl
  dtype: int
- id: name
  label: Name
  dtype: string
inputs:
- label: in
  domain: stream
  dtype: complex
- domain: message
  id: lock
- domain: message
  id: half_frame
outputs:
- label: out
  domain: stream
  dtype: complex
  vlen: ${ fftl }
file_format: 1
