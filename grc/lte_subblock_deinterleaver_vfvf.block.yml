id: lte_subblock_deinterleaver_vfvf
label: subblock deinterleaver vfvf
category: '[Lte]'
templates:
  imports: from gnuradio import lte
  make: lte.subblock_deinterleaver_vfvf(${num_groups}, ${items_per_group}, ${name})
parameters:
- id: num_groups
  label: Num_groups
  dtype: int
- id: items_per_group
  label: Items_per_group
  dtype: int
- id: name
  label: Name
  default: ' "subblock_deinterleaver_vfvf"'
  dtype: string
inputs:
- label: in
  domain: stream
  dtype: float
  vlen: ${ num_groups * items_per_group }
outputs:
- label: out
  domain: stream
  dtype: float
  vlen: ${ num_groups * items_per_group }
file_format: 1
