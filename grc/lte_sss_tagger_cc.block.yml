id: lte_sss_tagger_cc
label: sss tagger cc
category: '[Lte]'
templates:
  imports: from gnuradio import lte
  make: lte.sss_tagger_cc(${fftl}, ${name})
parameters:
- id: fftl
  label: Fftl
  dtype: int
- id: name
  label: Name
  dtype: string
inputs:
- label: in
  domain: stream
  dtype: complex
- domain: message
  id: frame_start
outputs:
- label: out
  domain: stream
  dtype: complex
file_format: 1
