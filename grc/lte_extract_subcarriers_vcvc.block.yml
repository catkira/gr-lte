id: lte_extract_subcarriers_vcvc
label: extract subcarriers vcvc
category: '[Lte]'
templates:
  imports: from gnuradio import lte
  make: lte.extract_subcarriers_vcvc(${N_rb_dl}, ${fftl}, ${name})
parameters:
- id: N_rb_dl
  label: N_rb_dl
  dtype: int
- id: fftl
  label: Fftl
  dtype: int
- id: name
  label: Name
  dtype: string
inputs:
- label: in
  domain: stream
  dtype: complex
  vlen: ${ fftl }
outputs:
- label: out
  domain: stream
  dtype: complex
  vlen: ${ 12 * N_rb_dl }
file_format: 1
