id: lte_sss_calculator_vcm
label: sss calculator vcm
category: '[Lte]'
templates:
  imports: from gnuradio import lte
  make: lte.sss_calculator_vcm(${fftl}, ${key_id}, ${key_offset}, '${id}')
parameters:
- id: fftl
  label: Fftl
  dtype: int
- id: key_id
  label: cell ID group key
  dtype: string
- id: key_offset
  label: offset marker key
  dtype: string
inputs:
- label: in
  domain: stream
  dtype: complex
  vlen: '72'
outputs:
- domain: message
  id: cell_id
- domain: message
  id: frame_start
file_format: 1
