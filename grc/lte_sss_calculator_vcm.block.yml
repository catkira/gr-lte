id: lte_sss_calculator_vcm
label: sss calculator vcm
category: '[Lte]'
templates:
  imports: from gnuradio import lte
  make: lte.sss_calculator_vcm(${fftl}, ${key_id}, ${key_offset}, ${name})
parameters:
- id: fftl
  label: Fftl
  dtype: int
- id: key_id
  label: Key_id
  dtype: string
- id: key_offset
  label: Key_offset
  dtype: string
- id: name
  label: Name
  dtype: string
inputs:
- label: in
  domain: stream
  dtype: complex
  vlen: '72'
outputs:
- domain: message
  id: cell_id
- domain: message
  id: frame_start
file_format: 1
