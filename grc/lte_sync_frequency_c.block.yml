id: lte_sync_frequency_c
label: sync frequency c
category: '[Lte]'
templates:
  imports: from gnuradio import lte
  make: lte.sync_frequency_c(${sig}, ${fftl}, ${name})
parameters:
- id: sig
  label: Sig
  dtype: raw
- id: fftl
  label: Fftl
  dtype: int
- id: name
  label: Name
  default: ' "sync_frequency_c"'
  dtype: string
inputs:
- label: in
  domain: stream
  dtype: complex
file_format: 1
