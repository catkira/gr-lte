id: lte_sync_frequency_c
label: CP FFO Calculator
category: '[Lte]'
templates:
  imports: from gnuradio import lte
  make: lte.sync_frequency_c(getattr(self, ${sig}), ${fftl}, ${known_f_offset}, ${name})
parameters:
- id: sig
  label: Sig
  dtype: raw
- id: fftl
  label: Fftl
  dtype: int
- id: known_f_offset
  label: known frequency offset
  dtype: float
- id: name
  label: Name
  default: '${id}'
  dtype: string
inputs:
- label: in
  domain: stream
  dtype: complex
file_format: 1
